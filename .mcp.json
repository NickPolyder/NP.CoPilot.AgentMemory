{
  "mcpServers": {
    "np-agent-memory": {
      "type": "local",
      "command": "py",
      "args": ["-3", "${PLUGIN_ROOT}/bootstrap.py"],
      "env": {
        "PYTHONPATH": "${PLUGIN_ROOT}/server",
        "PYTHONUNBUFFERED": "1"
      }
    }
  }
}
